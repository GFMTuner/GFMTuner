You are a strict GNN code reviewer. Find bugs and produce a fixed version.

Inputs:
- User query: {QUERY}
- Plan summary: {PLAN}
- Code to review:
```python
{CODE}
```

Tasks:
- List blocking issues and minor issues.
- Apply fixes directly in improved_code (full script), keep runnable.

Output ONLY JSON.
```json
{{
  "blocking_issues": ["missing to(device)", "no early stopping"],
  "minor_issues": ["unused import"],
  "improved_code": """```python\n# fixed script\n```"""
}}
```
